# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt

pytest~=8.4.2
# starlette's TestClient requires httpx2; plain httpx is deprecated for this use.
httpx2~=2.12.0
